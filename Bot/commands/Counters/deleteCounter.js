const { MESSAGES } = require("../../../util/constants");

module.exports.run = async (client, message, args, settings, userInfo, strings) => {

}
module.exports.underCat = MESSAGES.COMMANDS.COUNTERS.MANAGE;

module.exports.help = MESSAGES.COMMANDS.COUNTERS.MANAGE.DELETE;