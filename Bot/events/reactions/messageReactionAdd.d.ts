import { MessageReaction, User } from 'discord.js';
import { Client } from '../../../util/functions';

export declare function reactionAdd(client: Client, messageReaction: MessageReaction, user: User)